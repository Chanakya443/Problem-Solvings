class Solution {
    public int majorityElement(int[] nums) {
        int max=-10000;
        for(int i=0;i<nums.length;i++)
        {
            if(max<nums[i])
            {
                max=nums[i];
            }
        }
        
        return max;
        
    }
}
